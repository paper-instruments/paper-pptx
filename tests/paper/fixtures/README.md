# Fixture corpus

Frozen test corpus for the paper test suite. Read this before adding, changing, or trusting
any file here.

## The frozen-corpus rule

Every fixture and sidecar SHA-256 is pinned in `MANIFEST.sha256`
(`tests/paper/test_fixture_corpus.py` fails on any mismatch, unlisted fixture, missing or
schema-invalid sidecar, or orphan sidecar). Fixtures are **never regenerated by code under
test**. Changing or adding a fixture means, in one reviewed PR:

1. the fixture file itself,
2. its sidecar (`<stem>.json`, schema below),
3. a regenerated `MANIFEST.sha256`:
   `cd tests/paper/fixtures && find . \( -name "*.pptx" -o -name "*.json" \) | sed 's|^\./||' | sort | while read f; do shasum -a 256 "$f"; done > MANIFEST.sha256`

## Provenance buckets (directory = who actually wrote the bytes)

| Directory | Meaning |
| --- | --- |
| `office_authored/` | Authored in desktop Microsoft PowerPoint by a human (none yet) |
| `google_export/` | Exported from Google Slides (none yet) |
| `libreoffice_export/` | Final bytes written by LibreOffice Impress (here: headless round-trips of self-generated decks; the sidecar `provenance.notes` says exactly that) |
| `other_producers/` | Any other real-world producer (currently byte-for-byte Walnut Exporter packages copied from the MIT-licensed Paper Office eval corpus) |
| `self_generated/` | Built by `tests/paper/_authoring/build_fixtures.py` with this repo's own code |

Provenance honesty is non-negotiable: a file's bucket and sidecar describe how its bytes were
actually produced, never how we wish they had been. Code that only passes on self-generated
files is untested, so producer-authored fixtures remain required release evidence.

## Taxonomy coverage

| Taxonomy class | Fixtures |
| --- | --- |
| minimal-clean | `minimal_clean`, `lo_minimal_clean` |
| feature-isolated | `anchor_duplicate_text` (LS-01 structural identity), `diff_alignment_v1`/`diff_alignment_v2` (LS-05 independently authored paragraph and table insertion pair), `branded_template` (inherited size/font), `clrmap_remap`, `chart_notes` (clone fixture), `shared_media`, `autofit_none` / `autofit_normal` / `autofit_shape`, `autofit_inherited`, `whitespace_trailing_a`/`_b` (kernel trap pair), `sections`, `tables_in_group`, `nested_groups`, `hf_flags`, `merged_tables` (v0.11 tables), `footers_applied` (v0.11 fields/footers + hidden slide), `template_alpha`/`template_beta` (v0.11 import/rebind corpus), `lineage_v1`/`lineage_v2`/`lineage_reorder` (v0.11 diff ground truth), plus the `lo_*` producer variants |
| gauntlet | `gauntlet`, `scrub_gauntlet` (a deck carrying every delivery hazard at once: notes, comments, custom props, embedded font, unused-layout media), `walnut_chart_notes_absolute_rels`, `walnut_shared_media_absolute_rels` (unmodified producer packages retaining their broader package graphs) |
| corrupt-by-construction | `corrupt_dangling_sldid` (negative tests only) |
| large (perf smoke) | `large_smoke` |

A fixture is corrupt-by-construction iff any sidecar `features` entry starts with `"corrupt"`;
`tests.paper.corpus.is_corrupt_fixture()` is the single source of that rule. Corrupt fixtures
are never used as mutation targets.

## Sidecar schema (pinned by CONVENTIONS §4 — exactly these keys)

```json
{
  "fixture": "example.pptx",
  "provenance": {"app": "…", "version": "…", "notes": "…"},
  "features": ["…"],
  "ground_truth": {"…feature-keyed expected values…": "…"},
  "verified_by": "human name",
  "date": "YYYY-MM-DD"
}
```

Bootstrap sidecars carry `"verified_by": "UNVERIFIED - …"` because their ground truth was
machine-derived by direct XML inspection at authoring time and no human has verified it yet.
Where a claim is mechanically checkable, `test_fixture_corpus.py` re-verifies it against the
actual bytes on every run. Human verification, especially of effective-value expectations that
need desktop PowerPoint, means replacing the `verified_by` marker with the reviewer's name and
verification date.

## Golden-file policy

Goldens update only through `tests/paper/_authoring/update_goldens.py`, and their diffs are
human-reviewed in the PR. "No fix without a fixture": every bug found downstream becomes a
frozen fixture plus a failing test before its fix merges.
